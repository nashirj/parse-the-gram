# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'parse-the-gram' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for parse-the-gram

  pod 'Parse'
  pod 'AlamofireImage'
  pod 'MessageInputBar'

  target 'parse-the-gramTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'parse-the-gramUITests' do
    # Pods for testing
  end

end
